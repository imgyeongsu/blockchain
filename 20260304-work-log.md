# 2026-03-04 작업 로그

## 요약
피어 발견(Peer Discovery) 및 블록 동기화(IBD) 구현, 3노드 합의 테스트 성공

---

## 1. Git Merge Conflict 해결

**파일**: `jackpotchain/gacha/game.py`

**문제**: `dev` 브랜치와 `origin/dev`가 동일한 import 줄을 다르게 수정
- HEAD: `sha256` import 추가
- origin/dev: `validate_address` import 추가

**해결**: `validate_address`만 유지 (코드에서 실제 사용됨)

**커밋**: `806c80a`

---

## 2. 피어 발견 (Peer Discovery) 구현

### 신규 파일
| 파일 | 설명 |
|------|------|
| `network/discovery.py` | DNS 시드 조회, 피어 캐시(peers.json) 저장/로드 |
| `tests/test_discovery.py` | 피어 발견 테스트 (25개 케이스) |

### 수정된 파일
| 파일 | 변경 내용 |
|------|----------|
| `network/protocol.py` | `NetAddress`, `AddrMessage` 클래스 추가 |
| `network/node.py` | GETADDR/ADDR 핸들러, 주기적 발견 루프 |
| `network/__init__.py` | 새 클래스 export |

### 작동 흐름
```
노드 시작 → 캐시 로드 → 시드 조회 → 피어 연결
    → VERACK 후 GETADDR 전송 → ADDR 수신 → 캐시 저장
    → 30분마다 랜덤 피어에게 GETADDR 요청
```

**커밋**: `1737ac2`

---

## 3. 블록 동기화 (IBD) 구현

### 문제
node2가 중간에 연결되면 이전 블록 없이 새 블록만 받아서 "Previous block not found" 에러 발생

### 해결
| 파일 | 변경 내용 |
|------|----------|
| `consensus/chain.py` | `get_block_locator()` 메서드 추가 |
| `network/node.py` | VERACK 후 피어 높이 비교 → GETBLOCKS 요청 |
| `network/node.py` | 블록 수신 시 이전 블록 부재 → 동기화 요청 |

**커밋**: `7223b4e`

---

## 4. 멀티노드 테스트

### 테스트 스크립트
`jackpotchain/scripts/test_multinode.py`

### 사용법
```bash
# 터미널 1 (채굴 노드)
.venv/Scripts/python -m scripts.test_multinode node1

# 터미널 2
.venv/Scripts/python -m scripts.test_multinode node2

# 터미널 3
.venv/Scripts/python -m scripts.test_multinode node3
```

### 노드 설정
| 노드 | P2P 포트 | RPC 포트 | 역할 |
|------|---------|---------|------|
| node1 | 18333 | 18332 | 채굴자 (시드) |
| node2 | 18334 | 18335 | 팔로워 |
| node3 | 18336 | 18337 | 팔로워 |

### 테스트 결과
```
┌─────────────────────────────────────────────────────────┐
│  3-Node Consensus Test                                  │
├─────────────────────────────────────────────────────────┤
│  Node1 (Miner)  → Height=11 | Peers=2 | Mined=11       │
│  Node2          → Height=11 | Received=11              │
│  Node3          → Height=11 | Received=11              │
├─────────────────────────────────────────────────────────┤
│  ✓ 채굴 작동                                            │
│  ✓ P2P 연결 (피어 발견)                                 │
│  ✓ 블록 브로드캐스트                                    │
│  ✓ 블록 동기화 (IBD)                                    │
│  ✓ 3노드 동일 체인 상태 유지                            │
└─────────────────────────────────────────────────────────┘
```

---

## 커밋 히스토리

| 해시 | 메시지 |
|------|--------|
| `806c80a` | Merge branch 'origin/dev' into dev |
| `1737ac2` | feat: 피어 발견 (Peer Discovery) 구현 |
| `7223b4e` | feat: 블록 동기화(IBD) 및 멀티노드 테스트 구현 |

---

## 다음 단계 (TODO)

- [ ] 트랜잭션 전파 테스트
- [ ] 체인 재구성(Reorg) 테스트
- [ ] 실제 네트워크 환경에서 NAT/방화벽 테스트
- [ ] DNS 시드 서버 구축 (운영 환경)
