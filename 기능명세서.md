# 기능명세서

## 1. 문서 목적
본 문서는 `jackpotchain` 폴더 외부 문서(루트/`learning-roadmap`/`docs`)를 근거로, `jackpotchain` 패키지의 파일 생성 및 구현 기준을 정의한다.
특히 가챠 시스템은 `learning-roadmap/phase-3-advanced/16-2-gacha-system-final.md`를 기준으로 로또형(6자리, 6개 블록 비교) 사양으로 명세한다.

## 2. 근거 문서 범위
- `README.md`
- `requirements.txt`
- `docs/legal-considerations.md`
- `learning-roadmap/README.md`
- `learning-roadmap/IMPLEMENTATION_SPEC.md`
- `learning-roadmap/phase-0-foundation/*.md`
- `learning-roadmap/phase-1-core/*.md`
- `learning-roadmap/phase-2-network/*.md`
- `learning-roadmap/phase-3-advanced/*.md`
- `learning-roadmap/phase-4-optimization/*.md`
- `learning-roadmap/phase-5-evolution/*.md`
- `learning-roadmap/phase-6-dashboard/*.md`

## 3. 구현 목표
- Python 기반 UTXO 블록체인 노드 구성요소를 `jackpotchain` 하위 모듈로 생성한다.
- 합의, 트랜잭션, 스크립트, 멀티에셋, 네트워크, 저장소, 지갑, RPC를 단계적으로 구현 가능하도록 파일 책임을 고정한다.
- 가챠(로또) 도메인은 치트 억제 메커니즘 포함 명확한 검증 규칙으로 구현한다.

## 4. 패키지/파일 생성 명세
아래 파일은 생성 대상이며, 각 파일의 최소 책임을 만족해야 한다.

### 4.1 코어 레이어
- `jackpotchain/crypto/hash.py`
  - `sha256`, `double_sha256`, `hash160`
- `jackpotchain/crypto/signature.py`
  - secp256k1 키 생성/서명/검증
- `jackpotchain/crypto/address.py`
  - Base58Check 주소 생성/검증
- `jackpotchain/crypto/merkle.py`
  - 머클루트 계산/증명 검증
- `jackpotchain/core/transaction.py`
  - `TxInput`, `TxOutput`, `Transaction`, 직렬화/해시
- `jackpotchain/core/block.py`
  - `BlockHeader`, `Block`, PoW 검증, 머클루트
- `jackpotchain/core/utxo.py`
  - UTXO 자료구조 및 잔액 조회
- `jackpotchain/script/opcodes.py`
  - 필수 OP 코드 정의
- `jackpotchain/script/interpreter.py`
  - 스택 기반 스크립트 실행기
- `jackpotchain/script/standard.py`
  - 표준 스크립트(P2PKH/OP_RETURN 등) 생성기

### 4.2 합의/검증 레이어
- `jackpotchain/consensus/difficulty.py`
  - 목표 블록타임 기반 난이도 조정
- `jackpotchain/consensus/miner.py`
  - 코인베이스 생성, 블록 템플릿, nonce 탐색
- `jackpotchain/consensus/chain.py`
  - 체인 연결, 포크 처리, 블록 반영
- `jackpotchain/validation/transaction.py`
  - 트랜잭션 구조/문맥 검증
- `jackpotchain/validation/block.py`
  - 블록 헤더/PoW/TX 집합 검증

### 4.3 자산/가챠 레이어
- `jackpotchain/asset/manager.py`
  - JACK/POT 정책, 발행/소각 규칙
- `jackpotchain/asset/exchange.py`
  - JACK↔POT 교환 TX 생성/검증
- `jackpotchain/gacha/pool.py`
  - 잭팟 풀 잔액/스냅샷 관리
- `jackpotchain/gacha/commit_reveal.py`
  - 커밋 데이터 모델 및 유효성 도우미
- `jackpotchain/gacha/game.py`
  - 로또 결과 계산, 등수/보상 결정
- `jackpotchain/gacha/service.py`
  - 커밋/클레임 유스케이스 오케스트레이션

### 4.4 네트워크/서비스 레이어
- `jackpotchain/network/protocol.py`
  - 메시지 프레임/직렬화
- `jackpotchain/network/peer.py`
  - 피어 상태/신뢰도 관리
- `jackpotchain/network/node.py`
  - 메시지 라우팅, 블록/TX 전파
- `jackpotchain/sync/manager.py`
  - IBD/헤더 우선 동기화
- `jackpotchain/storage/database.py`
  - 블록/인덱스 영속화
- `jackpotchain/mempool/pool.py`
  - 미확정 TX 저장/정렬/제거
- `jackpotchain/wallet/wallet.py`
  - 키 보관, 잔액 조회, TX 생성
- `jackpotchain/rpc/server.py`
  - JSON-RPC 엔드포인트 제공
- `jackpotchain/cli/main.py`
  - 노드/지갑/가챠 CLI 진입점

### 4.5 테스트
- `jackpotchain/tests/test_crypto.py`
- `jackpotchain/tests/test_core.py`
- `jackpotchain/tests/test_network.py`
- `jackpotchain/tests/test_gacha.py`
- `jackpotchain/tests/test_gacha_integration.py`
  - 위 테스트 파일은 모듈 생성과 동시에 최소 happy-path/invalid-path를 포함한다.

## 5. 공통 규약
- Python 타입힌트 필수.
- 합의 규칙 경계값(금액, 수수료, 높이, 타임아웃)은 상수화.
- 직렬화 포맷은 네트워크/저장소 전 구간에서 동일해야 함.
- 검증 실패는 코드/메시지로 분리(디버깅 가능성 확보).

## 6. 가챠(로또) 기능 명세 - Final(16-2) 기준

### 6.1 용어
- Commit 블록 높이: `N`
- 비교 블록: `N+5, N+10, N+15, N+20, N+25, N+30`
- 선택 숫자: 6자리 hex 배열 (`0x0`~`0xF`)

### 6.2 참여 흐름
1. 사용자 `100 JACK -> 1 POT` 교환
2. `1 POT`을 소모하는 Commit TX 생성 (버전 `3`)
3. Commit이 블록 `N`에 포함
4. `N+30` 이후 Claim TX 생성 가능 (버전 `5`)
5. Claim 유효구간: `N+30`부터 `N+80`까지

### 6.3 결과 계산
- 각 비교 블록 해시의 마지막 hex digit를 추출한다.
- 6개 자리별 일치 개수(`match_count`)를 계산한다.
- 등수/보상은 다음 표를 따른다.

### 6.4 보상 규칙
- 6개 일치(1등): Commit 시점 잭팟 풀 잔액의 50%
- 5개 일치(2등): `100,000 JACK`
- 4개 일치(3등): `20,000 JACK`
- 3개 일치(4등): `2,000 JACK`
- 2개 일치(5등): `300 JACK`
- 1개 일치(6등): `1 POT` 반환
- 0개 일치: 보상 없음

### 6.5 참가비 분배
- 참가비 기준: `100 JACK`
- 80%: 잭팟 풀 적립
- 19%: 소각
- 1%: 채굴자 보상

### 6.6 치트 억제 규칙
- 단일 블록 조작으로 전체 결과를 통제할 수 없도록 6개 비교 블록 분산 사용.
- 1등 지급액은 Claim 시점이 아닌 Commit 시점 풀 스냅샷 기준으로 계산.
- Commit UTXO는 소유자 서명으로만 Claim 가능(프론트러닝 방지).
- 기한 내 미청구(`N+80` 초과) Commit은 만료 처리.

### 6.7 검증 규칙
- Commit TX 검증
  - `1 POT` 소모 여부
  - 6자리 숫자 형식/범위 확인
  - Commit UTXO 잠금 조건 확인
- Claim TX 검증
  - 참조 Commit 존재 여부
  - 현재 높이가 `[N+30, N+80]` 범위인지
  - 비교 블록 해시 기반 결과 재계산
  - 산출 보상과 출력 금액 일치 여부
  - 1등일 때 풀 UTXO 입력/거스름돈 처리 일관성

## 7. RPC/CLI 명세 반영
- RPC는 최소 다음 기능을 지원해야 한다.
  - 체인/블록/멤풀/피어 조회
  - 지갑 잔액/전송
  - 가챠 커밋/대기목록/클레임
- CLI는 다음 작업을 지원해야 한다.
  - 노드 실행(채굴 on/off)
  - 지갑 주소 생성/조회
  - 가챠 참여(커밋), 결과 청구(클레임)

## 8. 수용 기준(DoD)
- 파일 구조가 4장 명세대로 생성되어 import 가능해야 한다.
- `pytest jackpotchain/tests -v` 기준 가챠 핵심 시나리오가 통과해야 한다.
- 가챠 시나리오 3종(꽝/일반 당첨/1등 풀 지급)과 만료 시나리오 1종이 자동화 테스트로 재현되어야 한다.
- 치트 방지 핵심 규칙(Commit 시점 스냅샷, Claim 윈도우, 서명 소유권)이 코드 검증 경로에 포함되어야 한다.

## 9. 비고
- `learning-roadmap/phase-4-optimization/18-performance-optimization.md`와 `18-performance-optimization (1).md`는 중복 문서이므로 하나를 기준으로 유지한다.
- 본 문서는 구현 시작 기준선이며, 실제 코드와 차이가 발생하면 문서와 테스트를 함께 갱신한다.
